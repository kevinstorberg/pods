# Branch folder - contains all generated content, workspaces, and temporary files
# This keeps the main repo clean while allowing LLMs to generate and work with files
branch/*
tmp/*

# Environment variables
.env

# Node.js dependencies
# package-lock.json
node_modules/*
npm-debug.log*
yarn-debug.log*
yarn-error.log*